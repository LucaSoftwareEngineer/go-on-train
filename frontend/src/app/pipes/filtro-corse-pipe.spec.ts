import { FiltroCorsePipe } from './filtro-corse-pipe';

describe('FiltroCorsePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroCorsePipe();
    expect(pipe).toBeTruthy();
  });
});
